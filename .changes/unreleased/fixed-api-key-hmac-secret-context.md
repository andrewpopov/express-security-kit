---
kind: fixed
summary: The default API-key security context now carries the record's per-key hmacSecret, so request signing resolves it without a custom onAuthenticated.
---

Describe the user-facing change in one short paragraph before releasing.
